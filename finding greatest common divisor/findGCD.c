#include <stdio.h>
#include <stdbool.h>
#include <string.h>

int gcd(int x, int y)
{

    return ;
}

int main(void)
{ 
    int x, y;
    printf("Enter two numbers: ");
    scanf("%d %d", &x, &y);

    int gcdResult;

    gcdResult = gcd(x, y);
    
    return 0;
}