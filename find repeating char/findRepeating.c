#include <stdio.h>
#include <stdbool.h>
#include <string.h>

int main(void)
{
    //the last position in the string is fixed for the null character \0
    //best solution would be to use hashtable - coming back after hashtable is complete.
   char str1[80],str2[80];

   return 0;
}